<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>IMS</title>   
   {{> styles}}
</head>

<body>

 <nav>   
        <div>
        <h4><b>Office Inventory</b></h4>
       </div>
      <div class="d-flex align-self-center">
        {{#if is_admin}}<a>Admin</a>{{else}}<a>Employee</a>{{/if}}
        {{#if logged_in}}   
        <button type="button" class="nav-button btn-no-style btn" id="logout">Logout</button>    
        <span class="rounded-circle">{{employee_name}}</span>
        {{/if}}
      </div>    
  </nav>
  
 <main class="row g-0" style="min-height: 100vh;">
      <aside class="col col-12 col-lg-2">
        {{#if is_admin}}
         <div class="d-flex tab">          
          <a href="/admin">Transactions</a>
        </div>
         <div class="d-flex tab">          
          <a href="/admin/allitems">All Items</a>
        </div> 
         <div class="d-flex tab">          
          <a href="/admin/viewsummary">Usage</a>
        </div>
         <div class="d-flex tab">          
          <a href="/admin/viewitemcategorysummary">Category Data</a>
        </div>
         <div class="d-flex tab">          
          <a href="/admin/category">Add Category</a>
        </div>
         <div class="d-flex tab">          
          <a href="/admin/item">Add Item</a>
        </div>
        {{else}}
         <div class="d-flex tab">
          <a href="/employee"> Dashboard</a>
         </div> 
         <div class="d-flex tab">          
          <a href="/employee/request">Request</a>
         </div>
         <div class="d-flex tab">         
          <a href="/employee/history">History</a>
         </div>
         <div class="d-flex tab">          
          <a href="/employee/info">info</a>
         </div>
        {{/if}} 
      </aside>

      <div class="col col-12 col-lg-10" style="min-width: 768px">
        {{{body}}}
      </div>
    </main>  

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
          integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
          crossorigin="anonymous"></script>

          
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
  
</body>

</html>
